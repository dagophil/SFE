#ifndef SFE_RESOURCE_MANAGER_HXX
#define SFE_RESOURCE_MANAGER_HXX

namespace sfe
{

    /// The resource manager makes sure that files such as images, sounds, etc. are loaded only once.
    class ResourceManager
    {

    };

} // namespace sfe

#endif
